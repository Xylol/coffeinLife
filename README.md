# coffeinLife

Track your daily caffeine intake and visualize how it decays over time.
Log each coffee with its time and mg, see a real-time decay curve based on configurable half-life (default 6h).
Data stays in your browser via localStorage. Export/import as JSON.
[Live demo](https://xylol.github.io/coffeinLife/)
